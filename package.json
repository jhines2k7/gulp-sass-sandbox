{
  "name": "gulp-sass-sandbox",
  "version": "0.1.0",
  "description": "Simple sandbox to experiment with gulp and sass",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James O. Hines",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-compass": "^2.1.0"
  }
}
